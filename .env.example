# Receiving address only. No private key or seed phrase is needed by this server.
PAY_TO=REPLACE_WITH_YOUR_EVM_WALLET_ADDRESS
KITE_NETWORK=testnet
PRICE_USD=0.01
FACILITATOR_URL=https://facilitator.pieverse.io/v2
HOST=127.0.0.1
PORT=8080
AUDIT_TIMEOUT_MS=30000
# Local install path. The Docker image uses /ms-playwright instead.
PLAYWRIGHT_BROWSERS_PATH=./.cache/ms-playwright
