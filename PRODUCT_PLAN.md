# Kite Web Check 产品范围

方向：x402 付费服务。面向 AI 编程 Agent，按次购买公开网页的浏览器验收结果。

## 第一版

Agent 提交网址、预期文字与 CSS 选择器，获得桌面/手机检查结果、截图、自包含 HTML 和 JSON 报告。

- POST /v1/audits，每次一个公开网页，最多两个视口。
- 检查 HTTP 状态、横向溢出、图片失败、脚本/控制台错误和失败请求。
- 最多 10 个文字断言和 10 个 CSS 选择器，结果分通过、失败、无法确定。
- 默认 0.01 美元/次，可配置；尚未验证付费需求或真实链上结算。
- 只读检查，不登录、不提交表单、不接收任意测试脚本。

## 计费

verify → 浏览器执行 → 报告生成 → settle → 返回结果。

发现网页缺陷仍是成功交付，会收费。HTTP 404/500 可以生成缺陷报告，其余行为检查标记无法确定。无法导航、浏览器崩溃、任务超时导致完整报告无法生成时，不请求结算。

结算超时可能产生不确定的链上结果，不自动重试。服务器不存储报告，因此付款后连接断开可能无法恢复结果；需在公开运营前评估并补齐。

## 后续

先完成部署和真实付款验收，再按需求增加报告对比、回归测试、有边界的交互步骤、异步队列和支付后结果恢复。

当前为本地 MVP，不是已上线服务；本地演示和模拟付款测试不能替代真实链上证据。
